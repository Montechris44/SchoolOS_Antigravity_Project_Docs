# Feature Loop

For each task:
READ → PLAN → IMPLEMENT → RUN → TEST → INSPECT → FIX → VERIFY → DOCUMENT → MARK DONE

A task cannot be marked DONE because code compiles alone. It must satisfy its acceptance criteria and relevant tests.
