# Data Privacy Rules
Student, parent and staff data is sensitive. Enforce tenant isolation. Minimize data collection and model context. Keep secrets server-side. Test RLS and authorization with cross-tenant negative cases. Avoid sensitive data in logs. Build auditable, reversible workflows for consequential changes.
